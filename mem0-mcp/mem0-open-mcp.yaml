server:
  host: "0.0.0.0"
  port: 8765
  user_id: "l4s3r"

llm:
  provider: "ollama"
  config:
    model: "qwen2.5:7b"
    base_url: "http://mem0-ollama:11434"

embedder:
  provider: "ollama"
  config:
    model: "nomic-embed-text"
    base_url: "http://mem0-ollama:11434"
    embedding_dims: 768

vector_store:
  provider: "qdrant"
  config:
    collection_name: "mem0_memories"
    host: "mem0-qdrant"
    port: 6333
    embedding_model_dims: 768
